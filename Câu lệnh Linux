# Document đầy đủ về từng câu lệnh
man caulenh

# list open files, List all network connections
# Hướng dẫn sử dụng: https://catonmat.net/unix-utilities-lsof
lsof -i 

# Xem thông tin một dịch vụ đang chạy
service mysqld status
servicectl mysqld status

# Xem các dịch vụ mạng đang chạy tulpn: -TCP, -UDP, -LISTENING, -PROTOCOL, -CONNECTION TCP (sudo: superuser do)
sudo netstat -tulpn

# Xem CPU chạy PS: PROCESS STATUS,  -AUX: All processes for Users X, The X means 'any of the users'.
ps aux | grep nginx
top

# Xem thông tin phiên bản OS
uname -a

# Xem ổ đĩa DISH FILE or FREE, -ah: all human
df -ah

# Xem dung lượng thư mục DISH USE
du -sh /home

# Xem các ổ đĩa được mount
ls /mnt
